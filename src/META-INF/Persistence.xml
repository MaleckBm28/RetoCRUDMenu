<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">

  <persistence-unit name="CRUD_PU" transaction-type="RESOURCE_LOCAL">
    <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

    <!-- ENTIDADES -->
    <class>model.Profile</class>
    <class>model.User</class>
    <class>model.Admin</class>
    <class>model.Product</class>
    <class>model.BoosterBox</class>
    <class>model.BoosterPack</class>
    <class>model.Card</class>
    <class>model.Purchase</class>

    <properties>
      <!-- Hibernate usa TU pool (ConnectionPool) mediante tu provider -->
      <property name="hibernate.connection.provider_class" value="dao.PoolConnectionProvider"/>

      <!-- Dialecto MySQL 8 -->
      <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/>

      <!-- Crear tablas desde las @Entity al arrancar -->
      <!-- Para desarrollo: create -->
      <!-- Para entrega estable (sin tocar BD): validate -->
      <property name="hibernate.hbm2ddl.auto" value="create"/>

      <!-- Reduce warnings feos al hacer drops/constraints -->
      <property name="hibernate.hbm2ddl.schema-generation.drop-source" value="metadata"/>

      <!-- Logs SQL -->
      <property name="hibernate.show_sql" value="true"/>
      <property name="hibernate.format_sql" value="true"/>

      <!-- Recomendado en MySQL -->
      <property name="hibernate.connection.charSet" value="UTF-8"/>
      <property name="hibernate.connection.useUnicode" value="true"/>

      <!-- (Opcional) Si quieres que no "escanee" paquetes -->
      <property name="hibernate.archive.autodetection" value="class"/>

    </properties>
  </persistence-unit>

</persistence>
